import { FiDroplet } from "react-icons/fi";
import ModulePlaceholder from "./ModulePlaceholder";

export default function FuelLogs() {
  return (
    <ModulePlaceholder
      icon={FiDroplet}
      title="Fuel Logs — not yet built"
      description="Fuel entry logging (liters, cost, date) per vehicle."
    />
  );
}
