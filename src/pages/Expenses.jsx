import { FiCreditCard } from "react-icons/fi";
import ModulePlaceholder from "./ModulePlaceholder";

export default function Expenses() {
  return (
    <ModulePlaceholder
      icon={FiCreditCard}
      title="Expenses — not yet built"
      description="Toll and other operational expense tracking, rolled into total operational cost per vehicle."
    />
  );
}
