import { FiSettings } from "react-icons/fi";
import ModulePlaceholder from "./ModulePlaceholder";

export default function Settings() {
  return (
    <ModulePlaceholder
      icon={FiSettings}
      title="Settings — not yet built"
      description="Account, organization, and notification preferences."
    />
  );
}
