import api, { mockDelay } from "./api";

// TODO: implemented in the reports module. Kept here now so the services/
// layer + folder structure matches the documented API contract from the
// start. Components should only ever import from files in this folder.
const USE_MOCKS = true;

export default {
  USE_MOCKS,
};
